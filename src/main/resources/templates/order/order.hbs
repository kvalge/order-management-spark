<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css"
          integrity="sha384-WskhaSGFgHYWDcbwN70/dfYBj47jz9qbsMId/iRN3ewGhXQFZCSftd1LZCfmhktB" crossorigin="anonymous">
    <title>Home</title>
</head>
<body style="margin: 0 auto; width: 960px; background-color: ghostwhite;">
<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link active" href="/home">Home Page</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/customer">Register</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/product">Products</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/order">Orders</a>
                </li>
            </ul>
        </div>
    </div>
</nav>
<div class="container-sm" style="text-align: center">
    <div class="d-flex justify-content-center align-items-center">
        <div class="content" style="margin: 30px;">
            <h1>Orders</h1>
            {{#if message}}
                <h4 style="font-weight: bold; color: limegreen; margin: 10px">{{message}}</h4>
            {{/if}}
            <div>
                <table class="table table-sm" style="text-align: center">
                    <thead class="thead-dark" style="height: 50px">
                    <tr>
                        <th>Product Name</th>
                        <th>Price</th>
                        <th>To Order</th>
                    </tr>
                    </thead>
                    {{#each product}}
                        <tr>
                            <td style="vertical-align:middle">{{this.name}}</td>
                            <td style="vertical-align:middle">{{this.price}}</td>
                            <td>
                                <form>
                                    <button type="button" onclick="location.href='/product/{{this.id}}';"
                                            class="btn btn-warning"
                                            style="margin-bottom: 5px; padding: 5px">Add to Cart
                                    </button>
                                </form>
                            </td>
                        </tr>
                    {{/each}}
                    <tr>
                </table>
                <hr>
            </div>
        </div>
    </div>
</div>
</body>
</html>