<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css"
          integrity="sha384-WskhaSGFgHYWDcbwN70/dfYBj47jz9qbsMId/iRN3ewGhXQFZCSftd1LZCfmhktB" crossorigin="anonymous">
    <title>Customer</title>
</head>
<body style="margin: 0 auto; width: 960px; background-color: ghostwhite;">
<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link active" href="/home">Home Page</a>
                </li>
            </ul>
        </div>
    </div>
</nav>

<div class="container-sm" style="text-align: center">
    <h2 style="margin: 30px">Update Product</h2>
    <div class="row justify-content-center">
        <form action="/product/{{product.id}}/update" method="post">
            <table id="product">
                <tr>
                    <td>Name</td>
                    <td>Unit Price</td>
                </tr>
                <tr>
                    <td><label>
                        <input type="text" name="name" placeholder="{{product.name}}" value="{{this.name}}">
                    </label></td>
                    <td><label>
                        <input type="text" name="price" placeholder="{{product.price}}" value="{{this.price}}">
                    </label></td>
                </tr>
            </table>
            <div>
                <button type="submit" value="Submit" class="btn btn-primary" style="margin: 20px">Update</button>
            </div>
            <hr>
        </form>
    </div>
</div>
</body>
</html>